# Description: Curses-based widget set for text terminals
# URL:         UNMAINTAINED
# Depends on:  ncurses

name=stfl
version=0.24
release=1
#source=http://www.clifford.at/stfl/stfl-$version.tar.gz
source=stfl-$version.tar.gz

build() {
	cd stfl-$version

	sed -i 's|ncursesw/||' stfl_internals.h
	sed -i 's|-Os -ggdb||' Makefile

	make prefix=/usr FOUND_SWIG=0 DESTDIR=$PKG install
}
