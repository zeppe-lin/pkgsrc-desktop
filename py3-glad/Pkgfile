# Description: Vulkan/GL/GLES/EGL/GLX/WGL Loader-Generator
# URL:         https://glad.dav1d.de/
# Depends on:  py3-jinja2

name=py3-glad
version=2.0.8
release=1
source=$name-$version.tar.gz::https://github.com/Dav1dde/glad/archive/refs/tags/v$version.tar.gz

build() {
	cd glad-$version

	gpep517 build-wheel --wheel-dir .dist --output-fd 3 3>&1 >&2
	/usr/bin/python3 -m installer -d $PKG .dist/*.whl

	/usr/bin/python3     -m compileall $PKG
	/usr/bin/python3 -O  -m compileall $PKG
	/usr/bin/python3 -OO -m compileall $PKG
}
