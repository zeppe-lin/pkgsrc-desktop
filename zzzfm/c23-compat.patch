diff --git a/src/main-window.c b/src/main-window.c
index d49b63a..5fef3d7 100644
--- a/src/main-window.c
+++ b/src/main-window.c
@@ -5125,7 +5125,7 @@ GtkWidget* main_task_view_new( FMMainWindow* main_window ) {
 
 // ============== socket commands
 
-gboolean bool( const char* value ) {
+static gboolean parse_bool( const char* value ) {
     return ( !( value && value[0] ) || !strcmp( value, "1") ||
                  !strcmp( value, "true") || !strcmp( value, "True") || !strcmp( value, "TRUE") ||
                  !strcmp( value, "yes")  || !strcmp( value, "Yes")  || !strcmp( value, "YES") );
@@ -5295,14 +5295,14 @@ _missing_arg:
         }
         else if ( !strcmp( argv[i], "window_maximized" ) )
         {
-            if ( bool( argv[i+1] ) )
+            if ( parse_bool( argv[i+1] ) )
                 gtk_window_maximize( GTK_WINDOW( main_window ) );
             else
                 gtk_window_unmaximize( GTK_WINDOW( main_window ) );
         }
         else if ( !strcmp( argv[i], "window_fullscreen" ) )
         {
-            xset_set_b( "main_full", bool( argv[i+1] ) );
+            xset_set_b( "main_full", parse_bool( argv[i+1] ) );
             on_fullscreen_activate( NULL, main_window );
         }
         else if ( !strcmp( argv[i], "screen_size" ) )
@@ -5442,7 +5442,7 @@ _missing_arg:
                     *reply = g_strdup_printf( _("zzzfm: invalid property %s\n"), argv[i] );
                     return 2;
                 }
-                xset_set_b_panel( j, "show", bool( argv[i+1] ) );
+                xset_set_b_panel( j, "show", parse_bool( argv[i+1] ) );
                 show_panels_all_windows( NULL, main_window );
                 return 0;
             }
@@ -5451,9 +5451,9 @@ _missing_arg:
             if ( !str )
                 goto _invalid_set;
             if ( use_mode )
-                xset_set_b_panel_mode( panel, str, main_window->panel_context[panel-1], bool( argv[i+1] ) );
+                xset_set_b_panel_mode( panel, str, main_window->panel_context[panel-1], parse_bool( argv[i+1] ) );
             else
-                xset_set_b_panel( panel, str, bool( argv[i+1] ) );
+                xset_set_b_panel( panel, str, parse_bool( argv[i+1] ) );
             update_views_all_windows( NULL, file_browser );
         }
         else if ( !strcmp( argv[i], "panel_hslider_top" ) ||  !strcmp( argv[i], "panel_hslider_bottom" ) ||  !strcmp( argv[i], "panel_vslider" ) )
@@ -5548,22 +5548,22 @@ _missing_arg:
         {
             if ( !strcmp( argv[i] + 5, "ascend" ) )
             {
-                ptk_file_browser_set_sort_type( file_browser, bool( argv[i+1] ) ?  GTK_SORT_ASCENDING : GTK_SORT_DESCENDING );
+                ptk_file_browser_set_sort_type( file_browser, parse_bool( argv[i+1] ) ?  GTK_SORT_ASCENDING : GTK_SORT_DESCENDING );
                 return 0;
             }
             else if ( !strcmp( argv[i] + 5, "natural" ) )
             {
                 str = "sortx_natural";
-                xset_set_b( str, bool( argv[i+1] ) );
+                xset_set_b( str, parse_bool( argv[i+1] ) );
             }
             else if ( !strcmp( argv[i] + 5, "case" ) )
             {
                 str = "sortx_case";
-                xset_set_b( str, bool( argv[i+1] ) );
+                xset_set_b( str, parse_bool( argv[i+1] ) );
             }
             else if ( !strcmp( argv[i] + 5, "hidden_first" ) )
             {
-                str = bool( argv[i+1] ) ? "sortx_hidfirst" : "sortx_hidlast";
+                str = parse_bool( argv[i+1] ) ? "sortx_hidfirst" : "sortx_hidlast";
                 xset_set_b( str, TRUE );
             }
             else if ( !strcmp( argv[i] + 5, "first" ) )
@@ -5586,11 +5586,11 @@ _missing_arg:
         }
         else if ( !strcmp( argv[i], "show_thumbnails" ) )
         {
-            if ( app_settings.show_thumbnail != bool( argv[i+1] ) )
+            if ( app_settings.show_thumbnail != parse_bool( argv[i+1] ) )
                 main_window_toggle_thumbnails_all_windows();
         }
         else if ( !strcmp( argv[i], "large_icons" ) ) {
-            xset_set_b_panel_mode( panel, "list_large", main_window->panel_context[panel-1], bool( argv[i+1] ) );
+            xset_set_b_panel_mode( panel, "list_large", main_window->panel_context[panel-1], parse_bool( argv[i+1] ) );
             update_views_all_windows( NULL, file_browser );
 
         }
diff --git a/src/ptk/ptk-dir-tree-view.c b/src/ptk/ptk-dir-tree-view.c
index a0c6ec5..3c3f70a 100644
--- a/src/ptk/ptk-dir-tree-view.c
+++ b/src/ptk/ptk-dir-tree-view.c
@@ -285,7 +285,7 @@ GtkTreeModel* get_dir_tree_model() {
 
     if ( G_UNLIKELY( ! dir_tree_model ) )
     {
-        dir_tree_model = ptk_dir_tree_new( TRUE );
+        dir_tree_model = ptk_dir_tree_new();
         g_object_add_weak_pointer( G_OBJECT( dir_tree_model ), ( gpointer * ) (GtkWidget *) & dir_tree_model );
     } else {
         g_object_ref( G_OBJECT( dir_tree_model ) );
diff --git a/src/ptk/ptk-dir-tree.c b/src/ptk/ptk-dir-tree.c
index 6a89b73..282c338 100644
--- a/src/ptk/ptk-dir-tree.c
+++ b/src/ptk/ptk-dir-tree.c
@@ -199,7 +199,7 @@ void ptk_dir_tree_finalize ( GObject *object )
     ( * parent_class->finalize ) ( object );
 }
 
-PtkDirTree *ptk_dir_tree_new ()
+PtkDirTree *ptk_dir_tree_new ( void )
 {
     PtkDirTree * tree;
     tree = ( PtkDirTree* ) g_object_new ( PTK_TYPE_DIR_TREE, NULL );
diff --git a/src/ptk/ptk-dir-tree.h b/src/ptk/ptk-dir-tree.h
index dcd6db7..3e36c01 100644
--- a/src/ptk/ptk-dir-tree.h
+++ b/src/ptk/ptk-dir-tree.h
@@ -60,7 +60,7 @@ struct _PtkDirTreeClass
 
 GType ptk_dir_tree_get_type (void);
 
-PtkDirTree *ptk_dir_tree_new ();
+PtkDirTree *ptk_dir_tree_new ( void );
 
 void ptk_dir_tree_expand_row ( PtkDirTree* tree,
                                GtkTreeIter* iter,
diff --git a/src/ptk/ptk-file-misc.c b/src/ptk/ptk-file-misc.c
index 6a3a2b3..68e7c2b 100644
--- a/src/ptk/ptk-file-misc.c
+++ b/src/ptk/ptk-file-misc.c
@@ -1221,8 +1221,7 @@ void on_opt_toggled( GtkMenuItem* item, MoveSet* mset )
 
 void on_toggled( GtkMenuItem* item, MoveSet* mset )
 {
-    //int (*show) () = NULL;
-    void (*show) () = NULL;
+    void (*show) ( GtkWidget* ) = NULL;
     gboolean someone_is_visible = FALSE;
     gboolean opts_visible = FALSE;
 
@@ -1286,11 +1285,11 @@ void on_toggled( GtkMenuItem* item, MoveSet* mset )
     // entries
     if ( xset_get_b( "move_name" ) )
     {
-        show = (GFunc)gtk_widget_show;
+        show = gtk_widget_show;
         someone_is_visible = TRUE;
     }
     else
-        show = (GFunc)gtk_widget_hide;
+        show = gtk_widget_hide;
     show( mset->label_name );
     show( mset->scroll_name );
     show( mset->hbox_ext );
@@ -1298,42 +1297,42 @@ void on_toggled( GtkMenuItem* item, MoveSet* mset )
 
     if ( xset_get_b( "move_filename" ) )
     {
-        show = (GFunc)gtk_widget_show;
+        show = gtk_widget_show;
         someone_is_visible = TRUE;
     }
     else
-        show = (GFunc)gtk_widget_hide;
+        show = gtk_widget_hide;
     show( mset->label_full_name );
     show( mset->scroll_full_name );
     show( mset->blank_full_name );
 
     if ( xset_get_b( "move_parent" ) )
     {
-        show = (GFunc)gtk_widget_show;
+        show = gtk_widget_show;
         someone_is_visible = TRUE;
     }
     else
-        show = (GFunc)gtk_widget_hide;
+        show = gtk_widget_hide;
     show( mset->label_path );
     show( mset->scroll_path );
     show( mset->blank_path );
 
     if ( xset_get_b( "move_path" ) )
     {
-        show = (GFunc)gtk_widget_show;
+        show = gtk_widget_show;
         someone_is_visible = TRUE;
     }
     else
-        show = (GFunc)gtk_widget_hide;
+        show = gtk_widget_hide;
     show( mset->label_full_path );
     show( mset->scroll_full_path );
 
     if ( !mset->is_link && !mset->create_new && xset_get_b( "move_type" ) )
     {
-        show = (GFunc)gtk_widget_show;
+        show = gtk_widget_show;
     }
     else
-        show = (GFunc)gtk_widget_hide;
+        show = gtk_widget_hide;
     show( mset->hbox_type );
 
     gboolean new_file = FALSE;
@@ -1348,15 +1347,15 @@ void on_toggled( GtkMenuItem* item, MoveSet* mset )
 
     if ( new_link || ( mset->is_link && xset_get_b( "move_target" ) ) )
     {
-        show = (GFunc)gtk_widget_show;
+        show = gtk_widget_show;
     }
     else
-        show = (GFunc)gtk_widget_hide;
+        show = gtk_widget_hide;
     show( mset->hbox_target );
 
     if ( ( new_file || new_folder ) && xset_get_b( "move_template" ) )
     {
-        show = (GFunc)gtk_widget_show;
+        show = gtk_widget_show;
         if ( new_file )
         {
             gtk_widget_show( GTK_WIDGET( mset->combo_template ) );
@@ -1369,7 +1368,7 @@ void on_toggled( GtkMenuItem* item, MoveSet* mset )
         }
     }
     else
-        show = (GFunc)gtk_widget_hide;
+        show = gtk_widget_hide;
     show( mset->hbox_template );
 
     if ( !someone_is_visible )
diff --git a/src/settings.c b/src/settings.c
index 883da4d..38f99e3 100644
--- a/src/settings.c
+++ b/src/settings.c
@@ -1834,9 +1834,9 @@ gboolean xset_get_bool( const char* name, const char* var ) {
 
 gboolean xset_get_bool_panel( int panel, const char* name, const char* var ) {
     char* fullname = g_strdup_printf( "panel%d_%s", panel, name );
-    gboolean bool = xset_get_bool( fullname, var );
+    gboolean value = xset_get_bool( fullname, var );
     g_free( fullname );
-    return bool;
+    return value;
 }
 
 int xset_get_int_set( XSet* set, const char* var ) {
@@ -6016,7 +6016,7 @@ gboolean xset_menu_keypress( GtkWidget* widget, GdkEventKey* event, gpointer use
 
 void xset_menu_cb( GtkWidget* item, XSet* set ) {
     GtkWidget* parent;
-    void (*cb_func) () = NULL;
+    void (*cb_func) ( GtkWidget*, gpointer ) = NULL;
     gpointer cb_data = NULL;
     char* title;
     XSet* mset;  // mirror set or set
